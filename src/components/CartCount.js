// import React, { useState } from 'react';
function CartCount(props) {
    // const [count, setCount] = useState();
    // console.log(props)
    // props.getCount(props.passCount);
    return(
        <span>
            {props.passCount}
        </span>
    )
}

export default CartCount;